[build]
  publish = "."
  command = "npm run build"

[dev]
  command = "npm run dev:server"
  port = 8888
  targetPort = 8000
  publish = "."

[build.environment]
  NODE_VERSION = "18"

[functions]
  directory = "netlify/functions"
